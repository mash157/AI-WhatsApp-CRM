version: '3.8'

services:
  mongodb:
    image: mongo:latest
    container_name: whatsapp_mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: whatsapp_admin
      MONGO_INITDB_ROOT_PASSWORD: whatsapp_secure_password_123
      MONGO_INITDB_DATABASE: whatsappdb
    volumes:
      - mongodb_data:/data/db
      - mongodb_config:/data/configdb
    networks:
      - whatsapp_network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  mongo-express:
    image: mongo-express:latest
    container_name: whatsapp_mongo_express
    restart: always
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: whatsapp_admin
      ME_CONFIG_MONGODB_ADMINPASSWORD: whatsapp_secure_password_123
      ME_CONFIG_MONGODB_URL: mongodb://whatsapp_admin:whatsapp_secure_password_123@mongodb:27017
      ME_CONFIG_BASICAUTH_USERNAME: admin
      ME_CONFIG_BASICAUTH_PASSWORD: admin123
    depends_on:
      - mongodb
    networks:
      - whatsapp_network

volumes:
  mongodb_data:
    driver: local
  mongodb_config:
    driver: local

networks:
  whatsapp_network:
    driver: bridge
